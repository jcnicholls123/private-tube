services:
  metube:
    image: ghcr.io/alexta69/metube:latest
    container_name: metube
    restart: unless-stopped
    ports:
      - "30094:8081"
    volumes:
      # Change this Windows path to wherever you want YouTube downloads stored.
      - C:/PrivateTube/downloads:/downloads

  private-tube:
    image: ghcr.io/jcnicholls123/private-tube:latest
    container_name: private-tube
    restart: unless-stopped
    ports:
      - "3020:3020"
    environment:
      PORT: "3020"
      MEDIA_DIR: /media
      DATA_DIR: /data
      AUTH_ENABLED: "true"
      METUBE_URL: http://metube:8081
      ALLOW_DELETE: "false"
    volumes:
      # Must be the same folder MeTube writes into.
      - C:/PrivateTube/downloads:/media:ro
      # Persistent PrivateTube users/settings/database.
      - C:/PrivateTube/data:/data
    depends_on:
      - metube
